#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main() 
{
    int vSize{};
    cin >> vSize;
    vector<int> v{};
    for (int i{0}; i < vSize; ++i) 
    {
        int x{};
        cin >> x;
        v.push_back(x);
    }
    sort(v.begin(), v.end());
    for (int i{0}; i < vSize; ++i) 
    {
        cout << v[i] << ' ';
    }
    return 0;
}
